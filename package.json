{
  "name": "scheduler-showdown",
  "private": true,
  "workspaces": ["engine", "server", "client"],
  "scripts": {
    "dev": "concurrently -n server,client \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w client",
    "start": "npm start -w server",
    "test": "npm test -w engine && npm test -w server"
  },
  "devDependencies": {
    "concurrently": "^10.0.0"
  }
}
